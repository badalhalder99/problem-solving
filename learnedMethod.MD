- String method:
- - split(), slice(), substring(), toUpperCase(), toLoweCase(), replace(), replaceAll(), match(), length(), toUpperCase(), toLowerCase(), for of loop, indexOf(), charAt(), match(), matchAll(), includes()









- Array method:
- join(), push(), length(), slice(), for of loop, indexOf(), includes()









- Js Global:
- - parseInt()






-Regular expression function:
- -- test() method:
- ---- test() is a function of javaScript regular expression.
- ---- /regex/.test(str) return true → if the string matches the regex pattern
- ---- /regex/.test(str) return false → if it does NOT match






- *************************************************************************************************************************
- If you want to loop through String and array → use for...of loop or manual for loop
- If you want to loop through objects → use for...in or Object.keys()
- Note: For object loop you can not use for..of loop




