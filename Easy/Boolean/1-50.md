/*
=====================================================================
🧠 JAVASCRIPT BOOLEAN ALGORITHM CATEGORIES (BEGINNER)
=====================================================================

These problems are grouped by logical patterns used in JavaScript.

Instead of memorizing each condition, focus on recognizing
the underlying boolean logic pattern.

Most beginner boolean problems fall into reusable categories.

=====================================================================
1️⃣ TYPE CHECKING ALGORITHMS
=====================================================================

PATTERN
-------
Check the data type of a value.

Very common in validation, APIs, and defensive programming.

Problems in this category:
--------------------------

• Check if value is a string
• Check if value is a number (not NaN)
• Check if value is a function
• Check if value is iterable
• Check if value is primitive
• Check if object is instance of Array

Examples:

typeof "hello" → "string"
typeof 123 → "number"

Array.isArray([]) → true

Core JavaScript skills used:
----------------------------

typeof
Array.isArray()
instanceof
Symbol.iterator

Category Name:
--------------
Type Checking Algorithms


=====================================================================
2️⃣ NUMBER VALIDATION & MATHEMATICAL CHECKS
=====================================================================

PATTERN
-------
Evaluate numbers using mathematical rules.

Very common in algorithm challenges.

Problems in this category:
--------------------------

• Check if number is even
• Check if number is odd
• Check if number is positive
• Check if number is negative
• Check if number is within range (1–100)
• Check if number is between -10 and 10
• Check if divisible by 3
• Check if multiple of 5
• Check if whole number
• Check if prime (basic)
• Check if power of 2

Examples:

4 % 2 === 0 → true (even)
7 % 2 !== 0 → true (odd)

Core JavaScript skills used:
----------------------------

modulus (%)
Number.isInteger()
Math functions
loops (for prime check)

Category Name:
--------------
Number Logic Algorithms


=====================================================================
3️⃣ STRING VALIDATION & PATTERN CHECKING
=====================================================================

PATTERN
-------
Analyze string content or structure.

Used heavily in form validation and parsing.

Problems in this category:
--------------------------

• String longer than 10 characters
• String contains "error"
• String starts with "http"
• String ends with ".js"
• String exactly 5 characters long
• String contains uppercase letter
• String only letters
• String only alphanumeric
• String is palindrome
• String is whitespace only
• String is valid boolean ("true"/"false")
• String contains balanced quotes
• String longer than its reverse (trick)

Examples:

"hello".length > 10 → false

"http://..." → startsWith("http")

Core JavaScript skills used:
----------------------------

.length
includes()
startsWith()
endsWith()
regex
toLowerCase()

Category Name:
--------------
String Pattern Algorithms


=====================================================================
4️⃣ ARRAY VALIDATION & ANALYSIS
=====================================================================

PATTERN
-------
Check array structure, contents, or ordering.

Common in data processing tasks.

Problems in this category:
--------------------------

• Check if array is empty
• Array has more than 3 elements
• Array contains a value
• Array contains only numbers
• Array is sorted ascending
• Array has duplicate values

Examples:

[].length === 0 → true

[1,2,3] → sorted

Core JavaScript skills used:
----------------------------

.length
includes()
every()
some()
Set
loops

Category Name:
--------------
Array Logic Algorithms


=====================================================================
5️⃣ OBJECT VALIDATION ALGORITHMS
=====================================================================

PATTERN
-------
Check object structure or properties.

Important for working with APIs and JSON.

Problems in this category:
--------------------------

• Check if object is empty
• Object has no own properties
• Object has specific property
• Object has exactly 3 keys
• Object is frozen

Examples:

Object.keys(obj).length === 0 → empty

Core JavaScript skills used:
----------------------------

Object.keys()
Object.hasOwn()
Object.isFrozen()

Category Name:
--------------
Object Validation Algorithms


=====================================================================
6️⃣ TRUTHY / FALSY & EXISTENCE CHECKS
=====================================================================

PATTERN
-------
Evaluate truthiness or existence of values.

Crucial for control flow and bug prevention.

Problems in this category:
--------------------------

• Value is falsy
• Value is truthy and not empty string
• Value is null or undefined
• Variable is defined and not null
• Value is NaN

Examples:

!value → falsy

value ?? default

Core JavaScript skills used:
----------------------------

Boolean()
!
??
Number.isNaN()

Category Name:
--------------
Truthiness Algorithms


=====================================================================
7️⃣ COMPARISON & EQUALITY CHECKS
=====================================================================

PATTERN
-------
Compare values using strict or logical rules.

Problems in this category:
--------------------------

• Two values strictly equal
• Two strings equal ignoring case
• Age is 18 or older

Examples:

a === b

str1.toLowerCase() === str2.toLowerCase()

Core JavaScript skills used:
----------------------------

===
>=
toLowerCase()

Category Name:
--------------
Comparison Algorithms


=====================================================================
📊 FINAL CATEGORIZATION MAP
=====================================================================

Category
--------

Type Checking
Problems:
string check
number check
function check
iterable check
primitive check
array instance

Number Logic
Problems:
even/odd
positive/negative
range checks
divisibility
prime
power of 2

String Patterns
Problems:
length checks
contains
starts/ends
regex validations
palindrome
whitespace

Array Logic
Problems:
empty
length
contains
sorted
duplicates

Object Validation
Problems:
empty object
has property
key count
frozen

Truthiness
Problems:
falsy
truthy
null/undefined
NaN

Comparison
Problems:
strict equality
case-insensitive equality
age check

=====================================================================
⭐ INTERVIEW INSIGHT
=====================================================================

Most boolean problems rely on a small set of logic patterns:

1️⃣ Comparison
   (===, >, <, >=)

2️⃣ Type Checking
   (typeof, Array.isArray)

3️⃣ Mathematical Rules
   (%, divisibility, ranges)

4️⃣ String / Pattern Matching
   (includes, regex)

5️⃣ Truthiness Evaluation
   (!value, Boolean())

If you master these, you can solve almost all
beginner-level boolean problems efficiently.

=====================================================================
*/
